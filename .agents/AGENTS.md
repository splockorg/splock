# Splock Agent Guidelines

This file contains rules, context, and environment guidelines for agentic assistants working in the `splock` repository. 

These rules will be automatically loaded into the agent's memory every time a session is started in this workspace.

## Project Overview
- **Name**: splock
- **Language**: Python
- **Environment**: SDK-backed flows (uses `anthropic` and `claude-agent-sdk`).

## Core Directives
*(Add your standing rules, architectural guidelines, or coding conventions here. For example, if you have rules like "FEK by default" from your other repos, define the splock equivalent here.)*

## Environment Setup
- Python dependencies are listed in `requirements-sdk.txt`.
