"""bin/_render_plan — JSON-canonical plan substrate render+verify modules.

Per implplan §B.impl.2 file tree. CLI entry is `main.py`. Shared with the
`bin/_verify_plan` thin wrapper which reuses these modules.
"""
