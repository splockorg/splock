# portability_scaffolding — orchestrator plan

<!-- This file is rendered from portability_scaffolding_orchestrator.json by bin/render_plan.
Do not edit this file directly outside the operator-notes anchor block
at the bottom. The renderer regenerates everything else on each run. -->

## Phase

Phase 2

## Plan reference

portability_scaffolding_plan.json

## Tasks

### TSC1 — SC1 — Host-neutral hook vocabulary (bin/_host/events.py)

- **Agent:** `coder` (model: `inherit`)
- **File paths touched:** `bin/_host/__init__.py`, `bin/_host/events.py`, `tests/host_conformance/__init__.py`, `tests/host_conformance/test_events.py`
- **Tests enabled:** `tests/host_conformance/test_events.py`
- **Test plan:**
  - `test_events_no_host_imports` — asserts: import bin._host.events and assert its module __dict__/source contains no reference to bin._hooks, bin._intent, claude_agent_sdk, or any bin._host.shim/transcript/transport sibling — the substrate's core SC1 import-isolation guard. (fixture: `the imported bin._host.events module object plus its source file`)
  - `test_events_frozen` — asserts: constructing then mutating any of HookEvent/HookDecision/HookOutcome raises dataclasses.FrozenInstanceError (frozen=True is load-bearing so a boundary value cannot be mutated in-flight and bleed state between hooks). (fixture: `instances of the three frozen dataclasses`)
  - `test_events_enum_closed_sets` — asserts: HookEventKind, ToolClass, and the HookDecision.verdict Literal expose exactly the specified members (warn present but asserted unused elsewhere); USER_PROMPT_SUBMIT/SUBAGENT_STOP are first-class members not dropped. (fixture: `the enum/Literal definitions in bin._host.events`)

### TSC2 — SC2 — Hook Shim port + hook-entry dispatcher + trampoline cutover (deny-parity gated)

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC1
- **File paths touched:** `bin/_host/shim.py`, `bin/hook-entry`, `bin/_hooks/security_dispatch.py`, `bin/security-dispatch.sh`, `tests/host_conformance/test_shim_claude.py`, `tests/host_conformance/test_deny_parity.py`
- **Tests enabled:** `tests/host_conformance/test_shim_claude.py`, `tests/host_conformance/test_deny_parity.py`
- **Call sites:**
  - `bin/_host/shim.py (new) — HostHookShim ABC parse_event(stdin_payload, env)->HookEvent, render_decision(decision, event)->HookOutcome, classmethod detect(env)->HostHookShim; ClaudeHookShim the only registrant parsing the CC PreToolUse/lifecycle envelope, rendering exit-0 + permissionDecision:deny JSON on stdout`
  - `bin/hook-entry (new, executable) — detect(os.environ) → parse_event → named core hook (HookEvent->HookDecision) → render_decision → emit HookOutcome`
  - `bin/_hooks/security_dispatch.py:85 _is_deny_envelope (permissionDecision==deny) — expose a pure security_decision(event: HookEvent)->HookDecision callable hook-entry invokes; keep the existing main() stdin path for back-compat`
  - `bin/security-dispatch.sh (editable) — body → exec "$CLAUDE_PLUGIN_ROOT/bin/hook-entry" security-dispatch; filename/registration unchanged so hooks/hooks.json is untouched`
- **Test plan:**
  - `test_deny_parity` — asserts: THE acceptance gate for the cutover — for a battery of golden CC PreToolUse stdin payloads that today produce a deny (sealed-path write, unsafe DDL, package-safety, claude-md-discipline), bin/hook-entry security-dispatch emits byte-for-byte identical stdout and identical exit code to the legacy python -m bin._hooks.security_dispatch path; no trampoline is considered cut over until this is green for it. (fixture: `golden CC PreToolUse deny stdin payloads run through both dispatch entrypoints`)
  - `test_shim_parse_event` — asserts: golden CC payloads map to the expected HookEvent with tool-name→ToolClass mapping (Edit|Write→FILE_WRITE, Read→FILE_READ, Bash→SHELL, Task→AGENT_SPAWN) and file_path/command/prompt/source normalization. (fixture: `golden CC PreToolUse/lifecycle payloads`)
  - `test_shim_render_decision` — asserts: HookDecision(deny) renders an exit-0 HookOutcome carrying the CC permissionDecision:deny JSON; allow renders a silent exit-0. (fixture: `HookDecision allow/deny instances`)
  - `test_shim_fail_open` — asserts: malformed/truncated stdin yields an ALLOW HookOutcome (exit 0, no stdout deny) plus a hook-log row and never raises — preserves the exit-0-always contract so a hook crash cannot block a legitimate tool call. (fixture: `malformed/truncated stdin payloads`)
  - `test_detect_registry` — asserts: CLAUDE_* env resolves ClaudeHookShim; an unknown host resolves to ClaudeHookShim plus a warning row (never misroutes to a non-Claude shim). (fixture: `env dicts with and without CLAUDE_* markers`)
  - `scope_note_sealed_trampolines` — asserts: Phase-0 scope is confined to the editable bin/security-dispatch.sh cutover; the 14 sealed hooks/*.sh trampolines stay on the legacy python -m path (zero behavior change) and are incremental follow-through, not part of SC2 acceptance; hooks/permissions.deny stays CC-only and unchanged. Verify sealed status before any hooks/** write. (fixture: `the 14 sealed hooks/*.sh trampolines + hooks/hooks.json (unmodified)`)

### TSC3 — SC3 — Transcript Provider port + hook_writer refactor to SessionFacts

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC1
- **File paths touched:** `bin/_host/transcript.py`, `bin/_intent/hook_writer.py`, `tests/host_conformance/test_transcript_claude.py`
- **Tests enabled:** `tests/host_conformance/test_transcript_claude.py`
- **Call sites:**
  - `bin/_host/transcript.py (new) — TranscriptProvider ABC (transcript_path, session_facts, subagent_records); frozen FileTouch/SubagentRecord/SessionFacts (all fields optional); ClaudeTranscriptProvider absorbing the scraping battery`
  - `bin/_intent/hook_writer.py:58 _claude_project_dir, :64 _jsonl_path, :68 _read_tail, :93-257 regex extractors (_extract_recent_prompts/_extract_tools_used_count/_extract_files_touched/_extract_todo_state/_extract_workflow_stage/_extract_signals), :259 _live_status_for, :379-458 _find_recent_subagent_file/_upsert_subagent scraping half → move into ClaudeTranscriptProvider`
  - `bin/_intent/hook_writer.py:284 _ensure_row_exists, :320 _apply_signals, :460-535 _cmd_* → reduce hook_writer to DB-upsert code consuming a SessionFacts/SubagentRecord`
  - `bin/_intent/hook_writer.py:12-13 fail-open contract — preserve verbatim`
- **Test plan:**
  - `test_session_facts_golden` — asserts: a golden CC jsonl fixture yields the expected SessionFacts (custom_title, git_branch, workflow_stage, recent_prompts, tools_used_count, files_touched, live_status) and subagent_records equality — so Phase-B intent columns do not drift from today's values. (fixture: `a golden CC transcript jsonl`)
  - `test_transcript_fail_open` — asserts: corrupt and empty/missing fixtures both return an empty SessionFacts and never raise (missing/corrupt jsonl degrades to empty facts rather than crashing the hook; preserves hook_writer.py:12-13 fail-open). (fixture: `a corrupt jsonl plus an empty/missing jsonl`)
  - `test_hook_writer_upsert_equivalence` — asserts: driving the refactored hook_writer from a SessionFacts produces the same DB-upsert calls (same columns/values) as the pre-refactor scraping path against the same fixture — a regression net that also proves scraping and DB-upsert are decoupled. (fixture: `a golden CC jsonl plus captured DB-upsert calls before and after the refactor`)
  - `recon_g5_flag_note` — asserts: record (do NOT implement) the recon-G5 flag: extraction.agent_sessions.claude_session_id / agent_subagents.parent_claude_session_id are henceforth read as 'host session id'; the rename/alias migration is deferred and recorded in SC9. (fixture: `the SC9 reservations doc as the recording target`)

### TSC4 — SC4 — Model Transport port + ClaudeTransport (wrap) + sanitize_schema + nested-session env scrub

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC1
- **File paths touched:** `bin/_host/transport.py`, `bin/_sdk_bridge.py`, `tests/host_conformance/test_transport_claude.py`, `tests/host_conformance/test_sanitize_schema.py`, `tests/host_conformance/test_nested_session_scrub.py`
- **Tests enabled:** `tests/host_conformance/test_transport_claude.py`, `tests/host_conformance/test_sanitize_schema.py`, `tests/host_conformance/test_nested_session_scrub.py`
- **Call sites:**
  - `bin/_host/transport.py (new) — ModelTransport ABC (complete/stream/spawn_agent/sanitize_schema); ModelRole enum; frozen ModelPin; ModelCatalog ABC; CompletionRequest/CompletionResult/AgentSpawnSpec; ClaudeTransport wrapping _sdk_bridge.SubscriptionClient for complete/stream and sdk_spawners for spawn_agent; the build-now pure CLI-dialect sanitize_schema transform; NESTED_SESSION_ENV_VARS constant + a shared pure scrub helper; ClaudeTransport.spawn_agent applies the scrub before delegating to sdk_spawners`
  - `bin/_sdk_bridge.py:309-343 _drive_query / :287-306 _force_subscription_auth — add the NESTED_SESSION_ENV_VARS scrub at the claude-subprocess spawn boundary as a second spawn-boundary env guard; import the shared constant so _sdk_bridge and transport agree on the exact eight names`
  - `bin/_sdk_bridge.py:199 strip_schema_meta_keys — stays the live Claude sanitizer; ClaudeTransport.sanitize_schema delegates to it; the new CLI-dialect sanitize_schema ships as a tested pure function but is NOT wired into any live transport (CodexTransport deferred)`
  - `bin/_sdk_bridge.py:517-522 injectable query_fn/options_cls seam; :86 _STRUCTURED_RETRY_SUBTYPE=error_max_structured_output_retries; :117 _AdaptedMessage; :365 _build_options calls strip_schema_meta_keys(output_format)`
  - `bin/_retry_loop/iteration_loop.py — NOT touched (wrap-not-rewrite; iteration_loop stays untouched)`
- **Test plan:**
  - `test_nested_session_scrub` — asserts: SC4 affirmative proof guarding three failure modes — using the injectable query_fn/options_cls seam (_sdk_bridge.py:517-522) and a captured-env stub for the spawn_agent path (no real claude launched): with all eight session-identity vars set in the parent env, drive both spawn paths and assert the effective child env excludes all eight (per NESTED_SESSION_ENV_VARS) → guards incomplete scrub; assert CLAUDE_PLUGIN_ROOT/CLAUDE_PLUGIN_DATA/CLAUDE_PROJECT_DIR survive → guards over-scrub; assert an env without the eight is byte-identical after scrub → guards behavior drift / no-op-when-unset. (fixture: `a parent env with the eight session-identity vars, the injected query_fn/options_cls, and a captured-env spawn stub (no real claude launched)`)
  - `test_sanitize_schema` — asserts: gates SC4 per SC10 — (i) the Claude strip_schema_meta_keys round-trip stays byte-stable against golden schemas including schemas/plan_v1.schema.json; (ii) the new CLI-dialect transform strips $schema/$id/$comment, recursively sets required=all-properties + additionalProperties:false, rewrites const→single-value enum, drops minLength/maxLength/pattern/minItems/maxItems/format, keeps enum; (iii) a canned gpt-5.6-style emission validates against the original strict schema. (fixture: `golden schemas (incl schemas/plan_v1.schema.json) plus a canned gpt-5.6-style emission`)
  - `test_transport_claude` — asserts: ClaudeTransport.complete/stream map _AdaptedMessage→CompletionResult (including subtype==error_max_structured_output_retries→schema_exhausted); spawn_agent delegates to sdk_spawners and returns a CompletionResult; sanitize_schema delegates to strip_schema_meta_keys for the Claude dialect. (fixture: `_AdaptedMessage samples including the structured-retry subtype`)
  - `regression_sdk_bridge_and_planner_transport` — asserts: existing tests/test_sdk_bridge_schema_meta.py and tests/test_planner_subscription_transport.py stay green (SC10 union) — the wrap does not regress the proven _AdaptedMessage/_STRUCTURED_RETRY_SUBTYPE mapping; the scrub's only intended behavior change is nested invocations going deadlock→success, a strict capability gain. (fixture: `the existing sdk-bridge schema-meta and planner-subscription-transport regression suites`)

### TSC5 — SC5 — ClaudeModelCatalog + roster schema_version:4 + verifier-independence contract

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC4
- **File paths touched:** `bin/_host/transport.py`, `agents/_roster.json`, `agents/verifier.md`, `tests/host_conformance/test_catalog_claude.py`, `tests/host_conformance/test_roster_schema.py`
- **Tests enabled:** `tests/host_conformance/test_catalog_claude.py`, `tests/host_conformance/test_roster_schema.py`
- **Call sites:**
  - `bin/_host/transport.py (editable, sequential after SC4) — add concrete ClaudeModelCatalog.resolve(role)->ModelPin reading agents/_roster.json routing; verifier pin fixed=True with env override ignored; overridable pins honor ModelPin.override_env preserving OVERNIGHT_* > auto-latest-Opus > default precedence`
  - `agents/_roster.json (SEALED — sanctioned edit path) — schema_version 3→4; additive routing mapping {role: {model_role, pin_policy in {fixed, env-overridable}, model_pins.{family}, override_env}}; subagents list unchanged`
  - `agents/verifier.md:5 (SEALED) — model: line stays the CC-native mirror of model_pins.claude (claude-haiku-4-5-20251001); likely no edit needed since the value is unchanged; flag only`
  - `bin/_planner/two_call.py:74 DEFAULT_PLANNER_MODEL, :497 OVERNIGHT_* precedence — do NOT delete (zero-behavior-change; call sites still read directly until routing is live)`
  - `bin/_qa/invoke.py:103 DEFAULT_QA_MODEL — do NOT delete`
  - `bin/sdk_spawners.py:1636 _DEFAULT_REVIEWER_MODEL=sonnet, :2204 _DEFAULT_CODER_MODEL=opus — do NOT delete`
  - `bin/_eli5/invoke.py:36 DEFAULT_ELI5_MODEL — do NOT delete; recon/qna/research carry no explicit pin`
- **Test plan:**
  - `test_catalog_verifier_independence` — asserts: gates SC5 per SC10 — every registered family returns fixed=True for ModelRole.VERIFIER; setting a verifier-override / OVERNIGHT_* env var does not change the resolved verifier model (protects Ralph-gate determinism/cost contract). (fixture: `ClaudeModelCatalog plus an env carrying verifier / OVERNIGHT_* overrides`)
  - `test_catalog_overridable_precedence` — asserts: for PLANNER/QA/CODER/REVIEWER, resolve() honors ModelPin.override_env and reproduces today's OVERNIGHT_* > auto-latest-Opus > default precedence chain. (fixture: `ClaudeModelCatalog plus env override permutations`)
  - `test_roster_schema_v4` — asserts: schema_version==4; every model_pins family key is a registered transport family; every pin_policy:fixed role has a pin for every shipped family; routing.verifier.model_pins.claude equals agents/verifier.md's model: line. (fixture: `agents/_roster.json (v4) plus agents/verifier.md`)
  - `test_model_role_completeness` — asserts: resolve the ModelRole set against the codebase — enumerate from the union of DEFAULT_*_MODEL constants and roster subagents and assert the empirically-resolved membership; this nails the 9-vs-10th count (decide whether recon/qna/research get own pins or map to a shared PLANNER-family default, and pin it). (fixture: `the DEFAULT_*_MODEL constants plus roster subagents`)
  - `regression_verifier_pin_and_roster_consumers` — asserts: tests/test_verifier_model_pin_required.py and roster consumers (tests/test_agent_twins_match_the_engine.py, tests/test_shipped_surfaces_have_engines.py) stay green (SC10 union); implementation-time check confirms none pins schema_version==3. (fixture: `the existing verifier-pin-required and roster-consumer suites`)

### TSC6 — SC6 — Transport registry + routing seam (StaticRouter) + Tier-A DI threading

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC1, TSC4, TSC5
- **File paths touched:** `bin/_host/registry.py`, `bin/_host/routing.py`, `bin/_host/transport.py`, `bin/_planner/two_call.py`, `bin/_qa/invoke.py`, `bin/_retry_loop/opus_adapter.py`, `bin/_chain_overnight/phase_spawn.py`, `bin/_retry_loop/main.py`, `tests/host_conformance/test_registry.py`, `tests/host_conformance/test_routing_static.py`
- **Tests enabled:** `tests/host_conformance/test_registry.py`, `tests/host_conformance/test_routing_static.py`
- **Call sites:**
  - `bin/_host/registry.py (new) — register_transport(family, transport)/get_transport(family)->ModelTransport, a plain dict; "claude" the only registrant`
  - `bin/_host/routing.py (new) — frozen RouteQuery(role, intent, intent_kind, subjects, slug, required_capabilities: frozenset[str]) + RouteDecision(family, rule_id, reason); TransportRouter ABC; StaticRouter returning RouteDecision(family="claude", rule_id="static-default"); two-stage contract: eligibility filter required_capabilities <= transport.capabilities() THEN rule pick`
  - `bin/_host/transport.py (editable) — add abstract capabilities()->frozenset[str] to ModelTransport and ClaudeTransport's concrete set (structured-output is a real discriminator)`
  - `bin/_planner/two_call.py:273 _default_client() — gains router: TransportRouter = StaticRouter(); builds RouteQuery(role=PLANNER, slug=…); derives the client from the routed transport`
  - `bin/_qa/invoke.py:269 _default_client() — gains the same optional param; RouteQuery(role=QA)`
  - `bin/_retry_loop/opus_adapter.py:114 build_adapters — gains router: TransportRouter = StaticRouter(); builds per-role RouteQuery for CODER/REVIEWER/VERIFIER; derives spawn_*_fn from routed_transport.spawn_agent`
  - `bin/_chain_overnight/phase_spawn.py:428 build_adapters(...) — passthrough of the default router`
  - `bin/_retry_loop/main.py:529 and :692 build_adapters(...) — passthrough`
  - `bin/_retry_loop/iteration_loop.py — NOT touched (already host-agnostic via injected spawn_*_fn/run_verify_fn)`
- **Test plan:**
  - `test_registry_roundtrip` — asserts: register_transport/get_transport round-trip; duplicate-family and unknown-family rejection. (fixture: `a fresh transport registry carrying the claude registrant`)
  - `test_static_router_all_roles` — asserts: StaticRouter.route() returns family="claude", rule_id="static-default" for every ModelRole. (fixture: `StaticRouter plus every ModelRole`)
  - `test_eligibility_filter` — asserts: the two-stage contract: a RouteQuery whose required_capabilities exceed ClaudeTransport.capabilities() is filtered out as a genuine subset check. (fixture: `a RouteQuery with required_capabilities beyond ClaudeTransport.capabilities()`)
  - `test_seam_behavior_parity` — asserts: under the default StaticRouter each of the three Tier-A seams behaves identically to pre-change (planner client, qa client, and the build_adapters trio); plus a stub-router redirect proof that a non-default router changes the selected transport with zero call-site edits. (fixture: `the default StaticRouter plus a stub redirect router applied across the three seams`)
  - `test_no_field_named_host` — asserts: static assertion that RouteQuery/RouteDecision expose no field named host — avoids collision with agent_sessions.host = socket.gethostname() (register.py:402). (fixture: `the RouteQuery/RouteDecision dataclass fields`)
  - `guard_iteration_loop_untouched` — asserts: documented review check — bin/_retry_loop/iteration_loop.py is not in the diff; the router selects family only, the family's ModelCatalog + OVERNIGHT_* pins resolve the model within family, and the router cannot reach the fixed verifier pin. (fixture: `the task diff`)

### TSC7 — SC7 — Skill routing metadata in skills/*/SKILL.md frontmatter

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC6
- **File paths touched:** `skills/code/SKILL.md`, `skills/develop-plan/SKILL.md`, `skills/implplan/SKILL.md`, `skills/plan/SKILL.md`, `skills/qa/SKILL.md`, `skills/qna/SKILL.md`, `skills/recon/SKILL.md`, `skills/research/SKILL.md`, `skills/review/SKILL.md`, `skills/test/SKILL.md`, `skills/wrap/SKILL.md`, `tests/host_conformance/test_skill_metadata.py`
- **Tests enabled:** `tests/host_conformance/test_skill_metadata.py`
- **Call sites:**
  - `skills/*/SKILL.md (SEALED — sanctioned edit path) — all 11 (code, develop-plan, implplan, plan, qa, qna, recon, research, review, test, wrap): add the Agent Skills metadata: string-map: splock-route-intent (defaults to skill name), splock-route-default-family: "claude", splock-route-allowed-families: "claude", splock-route-requires (capability tags), splock-route-subject-affinity (optional); space-separated lists per the allowed-tools idiom`
  - `Rollout: ONE skill first, run the --strict smoke battery, only then the remaining ten`
  - `tests/test_smoke_battery.py:148 — the claude plugin validate . --strict gate the one-skill-first check runs`
- **Test plan:**
  - `test_skill_metadata_governed` — asserts: parse every SKILL.md: each splock-route-requires tag is in the governed closed capability enum (the same one capabilities() draws from in SC6) and each family value is a registered transport family; splock-route-intent present (defaulting to skill name). (fixture: `all 11 skills/*/SKILL.md frontmatter blocks`)
  - `test_no_custom_keys_on_commands_agents` — asserts: no splock-route-* top-level keys were added to commands/*.md or agents/*.md (custom top-level frontmatter keys trip claude plugin validate . --strict; the metadata: string-map is the only sanctioned carrier). (fixture: `commands/*.md and agents/*.md frontmatter`)
  - `procedural_strict_gate_one_skill_first` — asserts: after the one-skill edit, run pytest tests/test_smoke_battery.py -k strict (the claude plugin validate . --strict gate at test_smoke_battery.py:148) before rolling out the remaining ten — converting the medium-confidence 'metadata survives --strict' assumption into an explicit empirical check; record in the doc/PR why the Agent Skills compatibility field was NOT used (prose 1-500 chars, not machine-matchable). CI-union enforcement of the --strict gate is owned by SC10. (fixture: `one edited SKILL.md plus the --strict smoke battery`)

### TSC8 — SC8 — Intent-registry routing fields (additive; JSONL + markers only)

- **Agent:** `coder` (model: `inherit`)
- **File paths touched:** `bin/_intent/register.py`, `bin/intent`, `bin/_intent/refusal.py`, `bin/_intent/settings.py`, `tests/host_conformance/test_intent_routing_fields.py`
- **Tests enabled:** `tests/host_conformance/test_intent_routing_fields.py`
- **Call sites:**
  - `bin/_intent/register.py:164 JSONL row + :521 marker payload — add subjects (JSON array, nullable/default empty) and nullable model_family/routing_rule_id keys to both; validate subjects against a governed vocabulary; explicitly assert no field is named host`
  - `bin/_intent/register.py:402 host = socket.gethostname() — the naming-collision hazard the no-field-named-host assertion guards against`
  - `bin/intent:260 register.run(...) — add the --subjects cybersecurity,frontend argument, threading it through`
  - `bin/_intent/refusal.py and/or bin/_intent/settings.py — a governed subjects vocabulary (a closed set or a routing.subjects settings knob per the three-layer resolver), which may start empty; KIND left untouched`
- **Test plan:**
  - `test_subjects_written_to_jsonl_and_marker` — asserts: --subjects cybersecurity,frontend writes the array to both the JSONL row and the marker payload; omission defaults to empty. (fixture: `bin/intent invocations with and without --subjects`)
  - `test_subjects_vocabulary_rejection` — asserts: when a vocabulary is configured, an unknown subject is rejected (closed-enum discipline preventing a typo'd subject from silently mis-routing later); an empty vocabulary permits (start-empty behavior). (fixture: `a configured vs an empty subjects vocabulary knob`)
  - `test_forensics_fields_nullable_roundtrip` — asserts: model_family/routing_rule_id round-trip when set and are absent/null when unset; when populated, model_family validates against the SC6 registry (soft coupling only when populated). (fixture: `register rows with and without model_family/routing_rule_id populated`)
  - `test_no_field_named_host` — asserts: negative assertion over the JSONL row and marker payload shape — no field is named host (avoids collision with register.py:402 socket.gethostname()). (fixture: `the JSONL row and marker payload shape`)
  - `regression_intent_sqlite_backend` — asserts: tests/test_intent_sqlite_backend.py stays green (SC10 union) — no DB column added; the additive fields are JSONL + markers only, and MySQL/RDS columns are deferred to the recon-G5 slug (recorded in SC9). (fixture: `the existing intent sqlite backend suite`)

### TSC9 — SC9 — Reservations, transport inventory & deferrals (documentation)

- **Agent:** `coder` (model: `inherit`)
- **File paths touched:** `docs/plans/portability_scaffolding/portability_scaffolding_reservations.md`, `docs/MULTI_ROUTING_ROADMAP.md`
- **Tests enabled:** _none_
- **Call sites:**
  - `docs/plans/portability_scaffolding/portability_scaffolding_reservations.md (new; NOT sealed — sealed globs match *_plan.md/*_orchestrator.md, not *_reservations.md)`
  - `docs/MULTI_ROUTING_ROADMAP.md (editable) — optionally append a one-line pointer`
  - `Content = the nine reserved surfaces (a)-(i): (a) routing.* settings namespace + routing_rules.json overlay; (b) schemas/routing_rules_v1.schema.json + its recorded decisions (missing-key = evaluate-false/skip never error; match fields implicit-AND only; optional reserved fallback_families ranked tuple, opt-in; as_of ISO-date staleness field; hard ban on numeric prices/quality scores in rules); (c) intent.routed reserved in the EVENT enum (naming only); (d) post-Phase-2 orchestrator_v1.schema.json::tasks[].agent_assignment.model_family bump; (e) recon-G5 RDS claude_session_id/parent_claude_session_id migration; (f) recon-G4 HostTooling.validate_packaging() fourth port (sdk_spawners.py:545 claude plugin validate seam); (g) four-seam transport inventory incl bin/_fleet/spawn.py:101 build_child_argv (reconciliation 3, Phase 5); (h) the 'model after, never import' policy with the CVE-2026-42208 (LiteLLM import-cost + supply-chain) rationale; (i) stickiness semantics (route once per phase/spawn, family held sticky) + the open RouteDecision.sticky_scope question`
- **Test plan:**
  - `verification_kind: artifact_review` — asserts: reviewed for completeness against the SC9 checklist: all nine reserved items (a)-(i) present with names matching the substrate verbatim, no implementation code introduced, filenames/enum members spelled exactly — nine-item presence + name-exactness. (fixture: `docs/plans/portability_scaffolding/portability_scaffolding_reservations.md`)

### TSC10 — SC10 — Conformance suite + zero-behavior-change / CI-green acceptance gate

- **Agent:** `coder` (model: `inherit`)
- **Depends on:** TSC1, TSC2, TSC3, TSC4, TSC5, TSC6, TSC7, TSC8
- **File paths touched:** `tests/host_conformance/conftest.py`, `tests/host_conformance/__init__.py`, `tests/host_conformance/test_events.py`, `tests/host_conformance/test_shim_claude.py`, `tests/host_conformance/test_deny_parity.py`, `tests/host_conformance/test_transcript_claude.py`, `tests/host_conformance/test_transport_claude.py`, `tests/host_conformance/test_sanitize_schema.py`, `tests/host_conformance/test_catalog_claude.py`, `tests/host_conformance/test_roster_schema.py`, `tests/host_conformance/test_registry.py`, `tests/host_conformance/test_routing_static.py`, `tests/host_conformance/test_intent_routing_fields.py`, `tests/host_conformance/test_skill_metadata.py`, `tests/host_conformance/test_nested_session_scrub.py`, `tests/test_smoke_battery.py`
- **Tests enabled:** `tests/host_conformance/test_events.py`, `tests/host_conformance/test_shim_claude.py`, `tests/host_conformance/test_deny_parity.py`, `tests/host_conformance/test_transcript_claude.py`, `tests/host_conformance/test_transport_claude.py`, `tests/host_conformance/test_sanitize_schema.py`, `tests/host_conformance/test_catalog_claude.py`, `tests/host_conformance/test_roster_schema.py`, `tests/host_conformance/test_registry.py`, `tests/host_conformance/test_routing_static.py`, `tests/host_conformance/test_intent_routing_fields.py`, `tests/host_conformance/test_skill_metadata.py`, `tests/host_conformance/test_nested_session_scrub.py`, `tests/test_smoke_battery.py`
- **Call sites:**
  - `tests/host_conformance/conftest.py (new; suite collection/fixtures — the only substantive new write)`
  - `tests/host_conformance/__init__.py (ensured present from SC1)`
  - `the 13 conformance test files are listed in file_paths_touched only to satisfy the tests_enabled selector-path contract; SC10 runs them as the union and must NOT weaken any upstream assertion`
  - `tests/test_smoke_battery.py (existing) — listed to gate the --strict union; SC10 does not edit its assertions`
- **Test plan:**
  - `test_suite_collectable` — asserts: the tests/host_conformance/ package collects and runs as one selector union with no import errors. (fixture: `the full host_conformance package`)
  - `gating_relationships_recorded` — asserts: test_deny_parity gates the SC2 hook-entry cutover; test_sanitize_schema gates SC4; test_catalog_* (verifier-independence) gates SC5; test_nested_session_scrub is a mandatory SC4 affirmative proof that must pass before the transport port is 'landed'. (fixture: `the per-SC gating tests (deny-parity, sanitize, catalog, nested-scrub)`)
  - `zero_behavior_change_union_green` — asserts: the whole conformance union plus tests/test_smoke_battery.py is green — the standard Claude Code path is unchanged; the only intended behavior delta anywhere is nested-session invocations going deadlock→success (SC4), a strict capability gain. (fixture: `the full conformance union plus tests/test_smoke_battery.py`)
  - `guardrail_no_upstream_assertion_weakening` — asserts: SC10 must not modify the assertions of tests/test_smoke_battery.py or any upstream conformance test; file_paths_touched lists them only because the tests_enabled contract requires each selector's path to appear in the same task, and depends_on (SC1-SC8) guarantees they already exist and pass individually — no concurrent-write collision. (fixture: `the upstream conformance tests plus test_smoke_battery.py assertions`)

## Junctions

| ID | After task | Kind |
|---|---|---|
| J-deny-parity-cutover | TSC2 | test_gate |
| J-transport-landed | TSC4 | test_gate |
| J-catalog-verifier-independence | TSC5 | test_gate |
| J-ci-green-acceptance | TSC10 | test_gate |

## Review agent prompt pointer

_None._

## Notes (human-authored)

<!-- BEGIN-HUMAN-NOTES (do not remove this comment; render_plan reads it as anchor) -->

_Add notes here; this section is preserved across renders._

<!-- END-HUMAN-NOTES (do not remove this comment) -->
